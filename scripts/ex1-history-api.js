// * HISTORY API EXERCISES


/**
 * Using the history API, go to the next page (if one is available). If there
 * is no next page, please print out "You are at the most recent page"
 */
function nextPage() {
    // TODO 
}


/**
 * Using the history API, go to the last page (if one is available). If there
 * is no last page, please print out "There is no page available to go back to"
 */
function lastPage() {
    // TODO
}


/**
 * Using the history API, please go back two pages (if available). If there are
 * not two pages to go back, print out "Invalid argument".
 */
function twoAgo() {
    // TODO
}


/**
 * Using the history API, please go forward two pages (if available). If there 
 * are not two pages to go forward to, print out "Invalid argument"
 */
function twoForward() {
    // TODO 
}
